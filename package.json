{
  "name": "event-tracker",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npx esbuild src/index.ts --tsconfig=tsconfig.json --platform=browser --format=esm --target=esnext --bundle --outfile=dist/app.js",
    "build:prod": "npm run build -- --minify",
    "build:dev": "npm run build -- --sourcemap",
    "serve": "npm run build:dev -- --watch --servedir=.",
    "check": "npx tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mngrm3a/event-tracker.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/mngrm3a/event-tracker/issues"
  },
  "homepage": "https://github.com/mngrm3a/event-tracker#readme",
  "devDependencies": {
    "esbuild": "^0.25.9",
    "typescript": "^5.9.2"
  }
}