// OUTER_RADIUS: Maximum length of a radial bar from the center.
// INNER_RADIUS: Minimum radius for the inner circle (where bars start).
// HOUR_LABEL_RADIUS: Radius for placing hour labels around the chart.
// VALUE_LABEL_RADIUS: Radius for placing value labels (bar values).
// ROTATION_OFFSET: Rotates the chart so the first hour bar is at the top (12 o'clock).
// RADIAL_BAR_OFFSET: Additional rotation to visually center wedges between labels.
export const OUTER_RADIUS = 180;
export const INNER_RADIUS = 80;
export const HOUR_LABEL_RADIUS = 190;
export const VALUE_LABEL_RADIUS = 100
export const ROTATION_OFFSET = -82.5;
export const RADIAL_BAR_OFFSET = 15;
export const SVG_NS = "http://www.w3.org/2000/svg";